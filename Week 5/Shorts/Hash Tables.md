Hash tables combine the random access of an array with the dynamism of a linked lists. So we can insert, delete, and look up with near constant time while mitigating the disadvantages of arrays and lists.

The trade off is that has tables are not great at ordering or sorting data. 